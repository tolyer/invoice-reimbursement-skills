# yonyou-invoice-pool 安装说明

> **当前版本：0.2.0（2026-09-06）**　变更历史见 `CHANGELOG.md`
> 0.2.0 修正了 0.1.0 中三处与实测矛盾的流程（新建票袋不是弹窗、票袋内没有「添加账单」、
> 空票袋清理路径），**请勿使用 0.1.0**。

把**一个**结构化发票目录，完整搬进用友云（YonBIP）发票池的**一个**新建票袋。
只做票袋，不碰报销单表头、不关联事项申请、不提交。

---

## 一、安装

### 方式 A：解压到用户级 skills 目录（推荐，所有项目可用）

```bash
# macOS / Linux
unzip yonyou-invoice-pool.zip -d ~/.workbuddy/skills/

# Windows PowerShell
Expand-Archive yonyou-invoice-pool.zip -DestinationPath "$env:USERPROFILE\.workbuddy\skills"
```

装完确认目录结构：

```
~/.workbuddy/skills/yonyou-invoice-pool/
├── SKILL.md                     # 主入口：工作流 + 硬性规则
├── CHANGELOG.md                 # 版本更新记录
├── 安装说明.md                   # 本文件
├── references/
│   ├── 平台操作手册.md
│   ├── 目录解析与字段口径.md
│   └── 复核与交付.md
└── scripts/
    └── scan_invoice_dir.py
```

### 方式 B：只给当前项目用

解压到项目目录的 `.workbuddy/skills/` 下即可。

---

## 二、依赖

| 依赖 | 说明 |
|---|---|
| Python 3.9+ | 只用于跑 `scripts/scan_invoice_dir.py` 解析目录，无第三方库 |
| Tabbit 浏览器 + `tabbit-cli` | 平台自动化操作；没有它只能手工照手册操作 |

确认 Python 可用：

```bash
python3 --version
```

如果应用使用托管 Python，请把文档中的 `python3` 替换为该环境显示的 Python 可执行文件路径。

---

## 三、输入的发票目录必须长这样

```
发票目录-0720-0806/
├── 01-飞机含专车-HKD2873.00/
│   ├── ★票据本体/
│   │   └── QQ邮箱-机票订单确认¥2484.pdf
│   └── ☆附件/
│       ├── 01-支付截图.PNG
│       └── 02-行程单.pdf
├── 02-住宿3557-HKD2606.14/
│   ├── ★票据本体/ …
│   └── ☆附件/ …
└── 报销材料核验台账-0720-0806.md   （可选，有则优先取日期/发生地/备注）
```

规则：
- 每行一个文件夹，命名 `NN-描述-HKD金额`；
- 夹内两级：`★票据本体/`（**只能 1 个文件**）和 `☆附件/`（可多个）；
- 目录还不是这个结构？先用 `invoice-organizer` skill 整理，别硬上。

---

## 四、怎么用

对话里直接说：

```
用 yonyou-invoice-pool 把 <发票目录路径> 录成票袋 <票袋名称>
```

它会：

1. 跑脚本解析目录，生成《票袋录入计划》（逐行清单 + 文件清单 + 数据缺口）；
2. **停下来等你确认**——没点头绝不动浏览器；
3. 确认后建票袋 → 逐行录入 → 传票据本体和附件 → 保存；
4. 交付逐行台账 + 单页 HTML 报告。

---

## 五、两条最贵的坑（先记住能省两小时）

1. **只点「保存」，绝不点「保存为草稿」**
   草稿只进草稿箱，不落票袋，列表页搜不到，等于白干。

2. **文件上传别用 Playwright 的 `setInputFiles()`**
   在这个平台会直接挂起。改用 JS 注入 `DataTransfer` + `dispatchEvent('change')`，
   代码见 `references/平台操作手册.md` 第 5 节。

---

## 六、环境适配

本 skill 的入口与控件选择器按**微众科技租户**实测填写（2026-09-06 验证），
记在 `references/平台操作手册.md` 的「环境备注」表。

换租户/换版本后若入口对不上，先跑手册第 1 节的「入口发现」脚本枚举候选入口，
确认后把结论写回那张表，下次就不用再问。
